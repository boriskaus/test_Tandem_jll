module test_Tandem_jll

# Deliberately empty: all of the work happens in test/runtests.jl, which installs the
# Tandem_jll under test (from GitHub in CI, or from TANDEM_JLL_LOCAL_PATH locally)
# and exercises its executables.

end # module
